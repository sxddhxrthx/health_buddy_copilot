import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.researchtwin.app',
  appName: 'Research Twin',
  webDir: 'dist',
  // Keep packaged web assets local. Configure VITE_API_BASE_URL at build time.
};
export default config;
